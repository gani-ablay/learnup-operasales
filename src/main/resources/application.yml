myConfig:
  logger:
    console:
      prefix: 'log'

spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/library'
    username: 'postgres'
    password: 'pool'
    driver-class-name: 'org.postgresql.Driver'
server:
  port: 8081